{
  "name": "user-report-to-github-issue",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "type": "module",
  "scripts": {
    "start": "node index.js",
    "dev": "nodemon index.js",
    "test": "npm run test:lint && npm run test:mocha && npm run test:coverage",
    "test:mocha": "mocha \"tests/**/*.test.js\" --reporter json --reporter-option output=reports/test-results.json",
    "test:lint": "prettier . -c -u",
    "test:coverage": "c8 --reporter=lcov --reporter=text mocha \"tests/**/*.test.js\"",
    "lint:fix": "prettier . -w -u"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "axios": "^1.10.0",
    "cheerio": "^1.1.0",
    "fastify": "^4.24.3"
  },
  "devDependencies": {
    "c8": "^10.1.3",
    "chai": "^5.2.0",
    "mocha": "^11.7.1",
    "nodemon": "^3.1.10",
    "prettier": "^3.6.2"
  }
}
